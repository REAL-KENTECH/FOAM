# Upstream optimizer tests retained for provenance

These files were copied unchanged from the uploaded optimizer snapshot. They
refer to the original internal/import layout and pre-reconstruction return
signatures, so they are **not** part of the active test suite. The reconstructed
controller and experiment contract are tested under the top-level `tests/`
directory.
